version: '3.8'

services:
  test_db:
    image: postgres:15-alpine
    container_name: library3_test_db
    environment:
      - POSTGRES_USER=test_user
      - POSTGRES_PASSWORD=test_pass
      - POSTGRES_DB=library3_test
    ports:
      - "5433:5432"